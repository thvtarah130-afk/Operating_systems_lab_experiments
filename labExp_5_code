#include<stdio.h>

int main()
{
    int n, i, j, temp;
    int bt[20], pr[20], wt[20], tat[20];
    char p[20];

    float avg_wt = 0, avg_tat = 0;

    printf("Enter the total number of Processes: ");
    scanf("%d", &n);

    printf("\nPlease Enter the Burst Time and Priority of each process:\n");

    for(i = 0; i < n; i++)
    {
        p[i] = 'A' + i;

        printf("\nEnter the details of the process %c\n", p[i]);

        printf("Enter the burst time: ");
        scanf("%d", &bt[i]);

        printf("Enter the priority: ");
        scanf("%d", &pr[i]);
    }

    // Sorting according to priority (smaller number = higher priority)
    for(i = 0; i < n - 1; i++)
    {
        for(j = i + 1; j < n; j++)
        {
            if(pr[i] > pr[j])
            {
                temp = pr[i];
                pr[i] = pr[j];
                pr[j] = temp;

                temp = bt[i];
                bt[i] = bt[j];
                bt[j] = temp;

                temp = p[i];
                p[i] = p[j];
                p[j] = temp;
            }
        }
    }

    wt[0] = 0;

    for(i = 1; i < n; i++)
    {
        wt[i] = wt[i - 1] + bt[i - 1];
    }

    for(i = 0; i < n; i++)
    {
        tat[i] = wt[i] + bt[i];

        avg_wt += wt[i];
        avg_tat += tat[i];
    }

    printf("\nProcess_name\tBurst Time\tWaiting Time\tTurnaround Time\n");
    printf("--------------------------------------------------------------\n");

    for(i = 0; i < n; i++)
    {
        printf("%c\t\t%d\t\t%d\t\t%d\n",
               p[i], bt[i], wt[i], tat[i]);

        printf("--------------------------------------------------------------\n");
    }

    printf("\nAverage Waiting Time : %f", avg_wt / n);
    printf("\nAverage Turnaround Time: %f\n", avg_tat / n);

    return 0;
}
